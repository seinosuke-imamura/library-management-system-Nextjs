# Next.js 学習メモ

概念的な質問やつまずきの Q&A を追記する。進捗管理・API 一覧・対応表は [docs/PROJECT.md](docs/PROJECT.md) と学習計画を参照。

---

### 5. `requireAuth(request)` の `request` はなに？

**Q:** `requireAuth(request)` の `request` って何を渡してるの？

**A:** Route Handler の引数として Next.js が渡してくれる **HTTP リクエスト本体**（Web 標準の `Request` オブジェクト）。

- `request.headers` から Cookie / Authorization などを読める
- `request.url` からクエリを読める（`searchParams`）
- `request.json()` で body を読める（POST/PUT など）

`requireAuth(request)` は主に **ヘッダー（Cookie）からトークンを読むため**に `request` を受け取っている。  
つまり「この API 呼び出しのリクエスト情報を、認証チェック関数に渡している」という意味。

---

### 7. `Request` と `request` を混同したときの型エラー

**Q:** `require-auth.ts` で「`Request` 型の引数に割り当てられない」「`new` を使うつもり?」と出る。

**A:** `Request`（大文字）は Web API の **コンストラクタ/型**、`request`（小文字）は Route Handler から渡される **実体インスタンス**。  
`requireAuth(Request)` のように大文字を渡すと「クラスそのものを引数に渡している」扱いになり、型エラーになる。

- 正しくは「Route Handler の引数 `request` をそのまま渡す」
- `requireRole` の中で `requireAuth` を呼ぶ設計にすると、責務が混ざりやすい。`requireAuth` と `requireRole` は分離して、Route Handler 側で順に呼ぶと整理しやすい

---

### 6. `Response | void` と early return の意味

**Q:** `requireRole` を `Response | void` にすると early return が楽、ってどういうこと？

**A:** `requireRole` の返り値を次の2択にしておく設計のこと。

- 認可 NG のとき: `Response`（403 など）を返す
- 認可 OK のとき: `void`（何も返さない）

これにすると呼び出し側で「戻り値が `Response` なら即 `return`」という書き方ができる。  
結果として、認可失敗時の分岐を先に終わらせて、以降の本処理（DB 更新など）をまっすぐ書ける。

つまり early return を使って、ネストを深くせずに「失敗ケースを先に抜ける」ための型設計。

---

## 学習記録

---

### 1. GET 一覧が `{ success: true, data: [] }` で空になる

**Q:** seed 済みなのに 0 件返る。

**A:** 一覧 API に `where(eq(books.title, q || ""))` のような検索条件を入れていた。`q` が無いとき `""` との完全一致になり、どの書籍にもマッチしない。

**N2-2（一覧）** では `where` なしの `db.select().from(books)`。検索は **N2-3** の `/api/books/search` で `like` を使う。

---

### 2. GET 検索で `Unexpected end of JSON input`

**Q:** `/api/books/search?q=Book` が 500。`request.json()` で SyntaxError。

**A:** GET リクエストに JSON ボディはない。クエリは **`new URL(request.url).searchParams.get("q")`** で取る。

| メソッド | データの取り方 |
|---------|--------------|
| GET | `searchParams`（URL クエリ） |
| POST / PUT | `await request.json()`（body） |

---

### 3. 存在しない id なのに 200 が返る（404 にならない）

**Q:** `/api/books/999` が `{ success: true, data: {} }` になる。

**A:** Drizzle の `select()` は **常に配列**を返す。0 件のとき `[]`。

- 空配列 `[]` は JavaScript では **truthy**（`![]` は `false`）
- `if (!book)` では 404 分支に入らない

**対処:** `book.length === 0` を判定するか、`(...)[0]` で 1 件取り出して `if (!book)` とする。404 レスポンスには `{ status: 404 }` も付ける。

---

### 4. zod の safeParse とは

**Q:** `safeParse` ってなに？

**A:** スキーマでデータを検証する zod のメソッド。`parse()` と違い、失敗時に **例外を throw せず** `{ success: true, data }` または `{ success: false, error }` を返す。

Route Handler ではバリデーション失敗 = クライアントの入力ミス = **400** にしたいので、`safeParse` + `if (!result.success)` パターンが向いている。

---

### 8. `Response.json` と `NextResponse` の使い分け

**Q:** `Response.json` と `NextResponse`、どっちを使うべき？

**A:** どちらも「HTTP レスポンスを返す」もの。`NextResponse` は Next.js が `Response` を拡張したもの。

| 場所 | おすすめ | 理由 |
|------|---------|------|
| Route Handler（`app/api/**/route.ts`） | **`Response.json`** | Web 標準。N2 からの `{ success, data }` と揃えやすい |
| middleware（`middleware.ts`） | **`NextResponse`** | `redirect()` / `next()` / `rewrite()` が使える |

- API で JSON を返す → `Response.json({ ... }, { status: 401 })`
- 未ログインでページを `/login` へ飛ばす → `NextResponse.redirect(new URL("/login", request.url))`
- 認証OKでそのまま通す → `NextResponse.next()`

`NextResponse.json()` もあるが、API 層では既存の `Response.json` で十分。middleware で JSON を返す用途は通常ない。

---

### 9. `NextResponse` の特徴

**Q:** `NextResponse` の特徴は？

**A:** ブラウザ標準の `Response` に、Next.js 向けの便利メソッドが足されたもの。middleware や Server 側で「次にどう処理するか」を指示しやすい。

| メソッド | 役割 | このプロジェクトでの例 |
|---------|------|------------------------|
| `next()` | そのまま次の処理へ（ページ/API 本体へ） | ログイン済みで `/books` に通す |
| `redirect(url)` | 別 URL へ転送（302 など） | 未ログイン → `/login` |
| `rewrite(url)` | ブラウザ URL はそのまま、内部だけ別パスで描画 | N3 では未使用 |
| `json(body, init?)` | JSON レスポンス（`Response.json` 相当） | API では `Response.json` で十分 |

**`Response` との関係:** `NextResponse` は `Response` のサブクラス。`instanceof Response` は true。headers や status も通常の Response と同じ。

**middleware で使う理由:** 「JSON を返して終わり」ではなく、「通す / 飛ばす / 裏で別ルートを描画する」制御が主だから。認証ガードは `redirect` + `next` の組み合わせが典型。

**注意:** middleware は Edge で動く。`NextResponse` 自体は使えるが、DB や Node 専用モジュールは import しない（JWT 検証は `jose` のみ等）。

---

### 10. middleware で出たエラー（原因・理由・対処）

**Q:** N3-5 で `npm run dev` や `/books` アクセス時に middleware 関連のエラーが出た。原因と対処は？

**A:** 主に3種類。いずれも **middleware が Edge Runtime で動く** ことと、**import の連鎖** が原因。

#### (1) `source must start with / at "matcher[1]"`

| 項目 | 内容 |
|------|------|
| **原因** | `config.matcher` のパスが `/` で始まっていない |
| **例** | `"/books/:path*"` は OK、`"rentals/:path*"` は NG |
| **理由** | Next.js の matcher は URL パス形式（必ず `/` 始まり）を要求する |
| **対処** | すべて `"/rentals/:path*"` のように先頭 `/` を付ける |

#### (2) `Cannot read properties of undefined (reading 'reduce')` at `import 'dotenv/config'`

| 項目 | 内容 |
|------|------|
| **原因** | middleware → `jwt.ts` を import → ファイル先頭の `dotenv/config` が Edge で実行される |
| **理由** | `dotenv` は Node 向け。Edge には同等の仕組みがなく、モジュール評価時に落ちる |
| **対処** | ① middleware から `jwt.ts` を import しない ② Route Handler 用 `jwt.ts` からも `dotenv/config` は削除（Next.js が `.env.local` を読む） |

#### (3) `The edge runtime does not support Node.js 'fs' module`

| 項目 | 内容 |
|------|------|
| **原因** | middleware → `jwt.ts` → `lib/db` → `better-sqlite3` → `fs` という import 連鎖 |
| **理由** | `verifyToken` 自体は DB を使わなくても、**同じファイルを import すると先頭の import も全部読み込まれる**。SQLite は Node/fs 依存のため Edge 不可 |
| **対処** | **`lib/auth/verify-token.ts` を新規作成**（`jose` の `jwtVerify` のみ）。middleware はここだけ import。API 側は従来の `jwt.ts`（sign / verify + DB） |

#### 覚えておくこと

```
Route Handler（Node）  →  DB / dotenv / bcrypt  OK
middleware（Edge）     →  jose の JWT 検証のみ。DB は require-auth（API）側
```

**Cookie 読み取り:** middleware では `request.cookies.get("token")` を使う（`next/headers` の `cookies()` より一般的）。

---

### 11. Server Component の fetch と Client の useEffect の違い

**Q:** 書籍一覧など、Server Component の fetch と Client の useEffect、どう違う？

**A:** どちらもデータ取得できるが、**いつ・どこで・何ができるか**が違う。

| 観点 | Server Component + `fetch` | Client Component + `useEffect` + `fetch` |
|------|---------------------------|------------------------------------------|
| 実行場所 | サーバー（レンダリング前） | ブラウザ |
| `"use client"` | 不要（デフォルトが Server） | 必要 |
| 取得タイミング | ページ表示の前に完了 | 一度空の UI → 取得後に再描画 |
| `useState` / ボタン | そのままでは使えない | 使える |
| 検索・フォーム | 不向き（入力のたびに再取得は別設計） | 向いている |

**Server の流れ:** サーバーが API/DB からデータ取得 → HTML を組み立て → ブラウザに送る。初回からデータ入り HTML になりやすい（いわゆる SSR に近い）。

**Client の流れ:** まずコンポーネントがマウント → `useEffect` で `fetch` → `setState` → 再レンダリング。React 学習（Vite）と同じ感覚。

**このプロジェクト（N5）での目安:**

- 一覧の初回表示だけ Server `fetch` でもよい
- 検索ボックス・借りるボタン・エラー表示は Client
- 最初は Client + `useEffect` の方が React 計画の知識をそのまま使える。慣れたら Server 化を検討

**Cookie 認証:** どちらでも `fetch("/api/books", { credentials: "include" })` で Cookie を送る。Server Component の fetch も同様。

---

### 12. `"use client"` は何を宣言している？

**Q:** `"use client"` は何を宣言している？

**A:** そのファイル（と、そこから import される子コンポーネント）を **「ブラウザ（クライアント）で動かす React コンポーネント」** にする、という Next.js への指示。

- **宣言していないもの:** API ルート、DB、認証方式（Cookie 等）とは無関係
- **宣言しているもの:** 実行場所 = クライアント側。`useState` / `useEffect` / `onClick` / `onSubmit` が使える

**対比:**

| | Server Component（デフォルト） | Client Component（`"use client"`） |
|--|-------------------------------|-------------------------------------|
| 実行 | サーバー | ブラウザ |
| 例 | 静的表示、サーバーで fetch | フォーム、ボタン、入力 |

**境界:** `"use client"` はファイルの**先頭**に書く。そのファイルを import した親も Client 側のツリーになる（Client から Server は import できない）。

**N5-1 login:** フォーム送信・`useState`・`useRouter` が必要 → `app/login/page.tsx` の先頭に `"use client"` が必要。

---

### 13. AuthProvider（Context）とは何のため？

**Q:** AuthProvider がわからない。何をしている？

**A:** **ログイン中のユーザー情報（id, username, role）を、ページのあいだで共有する仕組み**。React の Context API を使う。

| 役割 | 例え |
|------|------|
| Cookie | 入館証（API・middleware が見る） |
| AuthProvider の `user` | 名札（画面が「ADMIN だからこのボタンを出す」と判断） |

**3つの部品:**

1. `createContext` … 共有ボックスを作る
2. `AuthProvider` … `user` state を持ち、`<AuthProvider>{children}</AuthProvider>` で子を包む
3. `useAuth()` … 子コンポーネントから `user` / `setUser` を読む hook

**データの流れ:**

```
login 成功 → setUser(data.data.user)
layout   → const { user } = useAuth() → role で Link を出し分け
```

**配置:** `app/layout.tsx` の `{children}` を `<AuthProvider>` で包む（login ページからも `setUser` できるように）。

**注意:** API レスポンス `data` に `push` しない。保存は `setUser(...)`。Cookie 認証は login API が自動で行う。

**Q（補足）:** `setUser(data.data.user)` でいい？  
**A:** **はい、それで正しい。** `data.data.user` が `{ id, username, role }` なら、そのまま `AuthUser` として渡せる。別途フィールドを1つずつ渡す必要はない。前提は API が `user` を返していること（`data.data.user` が `undefined` だと Context も空のまま）。

---

### 14. `metadata` と `"use client"` を両立するには？

**Q:** metadata を切り離すならどんなファイルにする？`use client` の layout で metadata を export するとエラーになる。

**A:** **`metadata` 用の別ファイルは基本作らない。** `metadata` は **Server Component の `layout.tsx` に残す**。Client 側にしたい処理だけを **別ファイル（`components/` など）に切り出す**。

| ファイル | `"use client"` | 役割 |
|----------|----------------|------|
| `app/layout.tsx` | なし（Server） | `export const metadata`、`html`/`body`、フォント、`AuthProvider` でラップ |
| `components/AuthProvider.tsx` | あり | Context |
| `components/ProtectedNav.tsx` など | あり | `useAuth`、nav、role 出し分け |
| `app/(protected)/layout.tsx` | なしでも可 | Server のまま Client コンポーネントを import して `{children}` と並べる |

**ルール:** `"use client"` を付けたファイルからは `metadata` / `generateMetadata` を export できない（ビルド前にサーバーで解決する必要があるため）。

**分割のイメージ:** layout は Server の殻、中身の nav だけ Client コンポーネントとして import。

---

### 15. hydration error とは？`<div>` を `<html>` の直下に置けない理由

**Q:** hydration error って何？「`<div>` cannot be a child of `<html>`」と出る。

**A:** **サーバーが出した HTML と、ブラウザ上で React が期待する DOM の形が食い違ったとき**に出る警告・エラー。Next.js では最初に Server で HTML を生成し、クライアント JS がその DOM にイベントを「載せる」（hydrate）処理をする。載せようとしたツリーと実際の DOM が違うと hydration error になる。

**今回の典型原因:** HTML のルールでは `<html>` の直下は `<head>` と `<body>` だけ。`<body>` なしで `{children}`（例: login の `<div>`）を `<html>` 直下に置くと、ブラウザが DOM を勝手に補正し、React の期待とずれる。

**正しい分担:**

| ファイル | 置くもの |
|----------|----------|
| `app/layout.tsx` | `<html>` → **`<body>`** → `AuthProvider` → `{children}` |
| `app/(protected)/layout.tsx` | `<body>` は置かない。`ProtectedNav` + `{children}` のみ |

`<body>` はアプリ全体で **1 回だけ**（ルート layout）。

---

### 19. Next.js 学習完了後の次のステップ

**Q:** フロント領域が苦手だと分かった。次に学ぶべきことは？

**A:** バックエンド方向を伸ばす場合の優先度。

| 優先度 | テーマ | 理由 |
|--------|--------|------|
| 1位 | PostgreSQL + 生 SQL | 本番で使われる DB。ORM に頼らず SQL を書ける力が差になる |
| 2位 | テスト（Vitest / Jest） | バックエンドで特に重視される。今のシステムをそのまま素材に使える |
| 3位 | デプロイ（Docker / Railway 等） | ローカルを本番公開する経験で全体像が見える |
| 4位 | Hono / Express 単体 | Next.js 内包ではなく純粋なバックエンドの仕組みを理解できる |

現時点で持っているスキル（TypeScript, React, Next.js, Drizzle ORM, JWT 認証）はバックエンド寄りの土台として十分。次の一手は **PostgreSQL + SQL** がコスパ最高。

---

### 18. Hono + React/Vite の構成でも HttpOnly Cookie は使えるか？

**Q:** Next.js にしなくても、Hono + Vite のまま HttpOnly Cookie を使えるの？

**A:** **技術的には可能**。HttpOnly Cookie は HTTP の仕組みであり、Next.js 固有ではない。Hono でも `Set-Cookie` ヘッダーを返せばよい。

ただし移行元の構成は**クロスオリジン**（フロント `localhost:5173` ↔ バック `localhost:3000`）なので、以下の設定が追加で必要になる。

| 必要な設定 | 理由 |
|-----------|------|
| `SameSite=None; Secure` | クロスオリジンで Cookie を送るため（HTTPS 必須） |
| `Access-Control-Allow-Credentials: true` | サーバー側 CORS 設定 |
| `Access-Control-Allow-Origin: http://localhost:5173` | `*` は資格情報付きでは使えない |
| fetch に `credentials: "include"` | フロント側で Cookie を送る指示 |

**Next.js フルスタックが楽な理由：** フロントとバックが同一オリジン（`localhost:3000`）になるため、`SameSite=Strict`（デフォルト）で Cookie が自動送信され、HTTPS もクロスオリジン設定も不要になる。

> HttpOnly Cookie 自体は Hono でも使えるが、クロスオリジン構成では設定が複雑になる。Next.js は「同一オリジンになる」という副作用でその複雑さを回避している。

---

### 17. Hono + React/Vite vs Next.js フルスタック — メリット・デメリット

**Q:** 移行元（Hono API + React/Vite + Drizzle/SQLite）と比べて、Next.js だけにまとめるとどう変わる？

**A:** 構成の変化を図にすると次のとおり。

```
【移行元】
ブラウザ ← fetch (絶対URL / Bearer Token in localStorage) →
React/Vite [frontend/]  ←→  Hono API [backend/]  ←→  SQLite

【移行先】
ブラウザ ← fetch (相対パス / HttpOnly Cookie) →
Next.js 単一アプリ: page.tsx + route.ts + lib/db/
```

#### メリット

| 観点 | 内容 |
|------|------|
| 構成のシンプルさ | プロジェクト・サーバーが 1 つになる |
| 認証の安全性 | localStorage → HttpOnly Cookie（XSS で読めない） |
| fetch URL | 絶対 URL → 相対パス（環境差異なし） |
| Server Components | DB に直接アクセスするコンポーネントを書ける（初期表示高速化） |

#### デメリット・注意点

| 観点 | 内容 |
|------|------|
| Server/Client 境界 | `"use client"` の付け忘れ・誤りでエラーが起きやすい |
| 分離しにくい | バックエンドを他クライアントと共有したい場合に不向き |
| サーバーレスとの相性 | `better-sqlite3` はファイル DB なので Vercel 等には向かない |
| ビルドの厳格さ | `npm run build` が型・import の不整合を洗い出す |

**小〜中規模アプリを少人数で作る**場合は構成シンプル・認証安全で Next.js フルスタックは合理的。「フロント/バックを独立スケールしたい」「複数クライアントで API を共有したい」なら Hono のような専用フレームワークの方が向いている。

---

### 16. `useParams()` の `id` が `string | string[]` になる理由

**Q:** `[id]` ルートなのに、なぜ `typeof id === "string"` を確認するの？

**A:** **実行時は通常 `string` だが、TypeScript の型は「あらゆる動的ルート」をカバーするため広く定義されている**から。

| フォルダ名 | URL 例 | `id` の実行時の型 |
|------------|--------|-------------------|
| `[id]` | `/books/1` | `string`（1セグメント） |
| `[...slug]`（キャッチオール） | `/books/a/b` | `string[]`（複数セグメント） |
| `[[...slug]]`（任意キャッチオール） | `/books` や `/books/a` | `string[]` または未定義 |

`useParams()` はページ共通の hook なので、「今どのフォルダ構造か」までは型で絞れない → `string | string[]`（+ `undefined` のことも）になる。

**`typeof id === "string"` の効果:**

1. TypeScript の型窄め（narrowing）— 以降 `id` を `string` として扱える
2. ガード — 配列のとき `` `/api/books/${id}` `` が `/api/books/1,2` のように壊れた URL になるのを防ぐ

`books/[id]/page.tsx` では実行時ほぼ常に `string` だが、**型の安全と fetch URL の防御**のために確認する、という位置づけ。

---

### 20. B1-2 の生 SQL はファイルを作らず psql にべた書きするの？

**Q:** B1-2 では SQL ファイルを作らず、psql に直接書くの？

**A:** **B1-2 の目的上、まずは psql に直接入力する**のが学習計画の想定。理由は次のとおり。

| やり方 | 向いている場面 |
|--------|----------------|
| psql に直接入力 | DDL/DML の意味を体で覚える・`\dt` ですぐ確認 |
| `.sql` ファイル + `psql -f` | 長い SQL の再利用・チーム共有・本番マイグレーション |
| Drizzle マイグレーション | B1-4 以降、アプリと DB スキーマをコードで管理 |

B1-2 で手打ちするのは **練習用の一時的なスキーマ**。リポジトリに残す必要はない。本番相当の管理は B1-4/5 で Drizzle が `drizzle/` 以下に SQL を生成する。

ファイルで書きたい場合は `psql -f schema.sql` や `\i schema.sql` でもよいが、**1 行ずつ実行して `\dt` で確認する**方が B1-2 の学びは深い。

---
